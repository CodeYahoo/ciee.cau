import type { Tractor } from '../../types'
import { Status } from '../common'
export function VehicleTable({tractors,selected,onSelect}:{tractors:Tractor[];selected?:string;onSelect:(id:string)=>void}){return <div className="vehicle-table"><div className="table-title"><strong>农机运行列表</strong><span>共 {tractors.length} 台</span><input aria-label="搜索农机" placeholder="搜索农机编号 / 操作人员"/></div><div className="table-head"><span>农机编号</span><span>状态</span><span>当前速度</span><span>行驶方向</span><span>操作人员</span><span>当前任务</span><span>任务进度</span></div>{tractors.slice(0,5).map(t=><button key={t.id} onClick={()=>onSelect(t.id)} className={`table-row ${selected===t.id?'active':''}`}><strong>🚜　{t.plate}</strong><Status value={t.status}/><span>{t.speed} km/h</span><span>{t.position.heading}°</span><span>{t.operator}</span><span>{t.task.name}</span><span>{t.task.progress}%</span></button>)}</div>}
